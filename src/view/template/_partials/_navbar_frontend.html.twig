<nav class="navbar navbar-front navbar-expand-lg">
	<div class="container">
		<a class="navbar-brand navbar-brand-front" href="{{ base_path }}home">WebDevPro</a>
		<button class="navbar-toggler navbar-toggler-front" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon">
				<i class="fas fa-bars"></i>
			</span>
		</button>
		<div class="navbar-collapse collapse" id="navbarNav">
			<ul class="navbar-nav py-2 m-auto">
				<li class="nav-item mx-0 mx-lg-1">
					<a class="nav-link nav-link-front py-2 px-0 px-lg-2" href="{{ base_path }}home">Home</a>
				</li>
				<li class="nav-item mx-0 mx-lg-1">
					<a class="nav-link nav-link-front py-2 px-0 px-lg-3" href="{{ base_path}}posts">Blog</a>
				</li>
			</ul>
			{% if session.user is not null %}
				<div class="navbar-nav ml-auto">
					<div class="nav-item dropdown">
						<a class="nav-link-front dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							 {% if session.user.profile_picture is not empty %}
								<img src="{{ base_path }}public/upload/avatar/{{ session.user.profile_picture }}" alt="user-pic" class="rounded-5" width="55">
							{% else %}
								<img src="{{ base_path }}public/img/default.png" alt="default-pic" class="rounded-5" width="55">
							{% endif %}
							<span class="ms-2">{{ session.user.username }}</span>
						</a>
						<div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
							<a class="dropdown-item dropdown-link" href="{{ base_path }}admin/dashboard">Dashboard</a>
							<a class="dropdown-item dropdown-link" href="{{ base_path }}logout">Logout</a>
						</div>
					</div>
				</div>
			{% else %}
				<ul class="navbar-nav">
					<li class="nav-item">
						<a class="nav-link-front py-2 px-0 px-lg-3" href="{{ base_path}}registration">Sign up</a>
					</li>
					<li class="nav-item">
						<a class="nav-link-front py-2 px-0 px-lg-3" href="{{ base_path}}login">Sign in</a>
					</li>
				</ul>
			{% endif %}
		</div>
	</div>
</nav>
