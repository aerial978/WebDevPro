<div class="form-group">
    <label for="{{ id }}" class="form-label">{{ label }}</label>
    <textarea name="{{ name }}" id="{{ id }}" class="{{ class }}" {% if readonly %}readonly{% endif %}>{{ value ?? '' }}</textarea>
</div>
